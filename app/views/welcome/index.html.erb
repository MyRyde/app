<h2>MyRyde</h2>
<p>
  Not the ride-sharing app that Austin needs. The ride-sharing app that Austin deserves.
</p>
<% if signed_in? %>
  <%= current_user.email %>
  <%= button_to "Sign out", sign_out_path, method: :delete %>
<% else %>
  <%= link_to "Sign in", sign_in_path %>
<% end %>